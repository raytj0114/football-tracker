開発サーバーを起動し、現在の開発状況を確認する。

## 実行内容
1. 依存関係がインストールされているか確認（なければ `npm install`）
2. 型チェック実行
3. 開発サーバー起動

## 手順

1. `node_modules` が存在しなければ `npm install` を実行
2. `npm run type-check` で型チェック
3. `npm run dev` で開発サーバー起動

## 確認事項
- `.env.local` が存在するか
- 必要な環境変数が設定されているか
- ポート3000が空いているか

$ARGUMENTS
